const initialstate = {
  places: [],
  selectedPlace: null
};

const reducer = (state = initialstate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
